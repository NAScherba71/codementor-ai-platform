# ╔═══════════════════════════════════════════════════════════════════════════╗
# ║  🚀 CodeMentor Frontend - Environment Configuration                      ║
# ║  Copy this file to .env.local and configure for your deployment          ║
# ╚═══════════════════════════════════════════════════════════════════════════╝

# ═══════════════════════════════════════════════════════════════════════════
# 🔧 Application Configuration
# ═══════════════════════════════════════════════════════════════════════════

NODE_ENV=development
PORT=3000

# ═══════════════════════════════════════════════════════════════════════════
# 🌐 Backend API URLs (Client-side - Public)
# ═══════════════════════════════════════════════════════════════════════════

# Backend API URL - **REQUIRED FOR PRODUCTION**
# This is the most important configuration for the frontend to work properly.
# The frontend uses this URL to communicate with the backend service.
#
# ⚠️  IMPORTANT: This is a build-time variable (NEXT_PUBLIC_* are embedded during build)
#     You must REBUILD the Docker image after changing this value!
#
# This is a client-side variable (NEXT_PUBLIC_*) exposed to the browser
# 
# Local development: http://localhost:3001
# Docker Compose: http://backend:3001
# Cloud Run: https://codementor-backend-{PROJECT_NUMBER}.{REGION}.run.app
#
# To get your Cloud Run backend URL after deployment:
#   gcloud run services describe codementor-backend --region=us-central1 --format='value(status.url)'
#
# Example: NEXT_PUBLIC_API_URL=https://codementor-backend-31499486874.us-central1.run.app
#
# ❌ PRODUCTION DEPLOYMENT: This MUST be set in Cloud Run environment variables
# or your application will fail with "Backend service not configured" error
NEXT_PUBLIC_API_URL=http://localhost:3001

# AI Engine URL (Direct access - optional)
# Local: http://localhost:5000
# Cloud Run: https://codementor-ai-engine-{PROJECT_NUMBER}.{REGION}.run.app
# Note: In production, frontend should access AI Engine through Backend
NEXT_PUBLIC_AI_API_URL=http://localhost:5000

# WebSocket URL
# Local: ws://localhost:3001
# Cloud Run: wss://codementor-backend-{PROJECT_NUMBER}.{REGION}.run.app
NEXT_PUBLIC_WS_URL=ws://localhost:3001

# AI Console API Base URL
# Used by Playground page for AI console endpoints (health, chat, analyze)
# Local: leave empty or http://localhost:3001
# Cloud Run: https://codementor-backend-{PROJECT_NUMBER}.{REGION}.run.app
NEXT_PUBLIC_API_BASE_URL=

# ═══════════════════════════════════════════════════════════════════════════
# ☁️ GCP Cloud Run Configuration (for production deployment)
# ═══════════════════════════════════════════════════════════════════════════

# Example Cloud Run URLs (replace with your actual URLs after deployment):
# NEXT_PUBLIC_API_URL=https://codementor-backend-31499486874.us-central1.run.app
# NEXT_PUBLIC_AI_API_URL=https://codementor-ai-engine-31499486874.us-central1.run.app
# NEXT_PUBLIC_WS_URL=wss://codementor-backend-31499486874.us-central1.run.app
# NEXT_PUBLIC_API_BASE_URL=https://codementor-backend-31499486874.us-central1.run.app

# ═══════════════════════════════════════════════════════════════════════════
# 🔐 API Keys & Secrets (Server-side - Private)
# ═══════════════════════════════════════════════════════════════════════════

# Note: These are server-side only (not exposed to browser)
# In Cloud Run, set these via Secret Manager

# JWT Configuration
JWT_SECRET=your_super_secret_jwt_key_here
JWT_EXPIRES_IN=24h

# Code Execution API
JUDGE0_API_KEY=your_judge0_api_key_here

# Database Configuration (Server-side only)
MONGODB_URI=mongodb://localhost:27017/codementor-ai
REDIS_URL=redis://localhost:6379

# ═══════════════════════════════════════════════════════════════════════════
# 🔑 OAuth & Authentication
# ═══════════════════════════════════════════════════════════════════════════

GITHUB_CLIENT_ID=your_github_oauth_client_id
GITHUB_CLIENT_SECRET=your_github_oauth_client_secret

# ═══════════════════════════════════════════════════════════════════════════
# 📧 Email Configuration (optional)
# ═══════════════════════════════════════════════════════════════════════════

EMAIL_SERVICE=gmail
EMAIL_USER=your_email@gmail.com
EMAIL_PASS=your_app_password

# ═══════════════════════════════════════════════════════════════════════════
# 📊 Analytics & Monitoring (Public - Client-side)
# ═══════════════════════════════════════════════════════════════════════════

NEXT_PUBLIC_GOOGLE_ANALYTICS_ID=GA-XXXXXXXXX
NEXT_PUBLIC_MIXPANEL_TOKEN=your_mixpanel_token

# Feature Flags
NEXT_PUBLIC_ENABLE_ANALYTICS=false
NEXT_PUBLIC_ENABLE_PAYMENTS=false

# ═══════════════════════════════════════════════════════════════════════════
# 💳 Payment Processing (optional - for future monetization)
# ═══════════════════════════════════════════════════════════════════════════

NEXT_PUBLIC_STRIPE_PUBLIC_KEY=pk_test_xxxxxxxxx
STRIPE_SECRET_KEY=sk_test_xxxxxxxxx
STRIPE_WEBHOOK_SECRET=whsec_xxxxxxxxx

# ═══════════════════════════════════════════════════════════════════════════
# 🐛 Development & Debugging
# ═══════════════════════════════════════════════════════════════════════════

NEXT_PUBLIC_DEBUG_MODE=true

# ═══════════════════════════════════════════════════════════════════════════
# 📝 Notes
# ═══════════════════════════════════════════════════════════════════════════

# 1. Environment Variable Visibility in Next.js:
#    - NEXT_PUBLIC_* → Exposed to browser (client-side)
#    - Without prefix → Server-side only (secure)
#
# 2. Cloud Run Deployment:
#    - Get service URLs from: gcloud run services list
#    - Or from Terraform outputs after deployment
#    - Update NEXT_PUBLIC_API_URL and related URLs
#
# 3. Service URLs Format:
#    https://{SERVICE_NAME}-{PROJECT_NUMBER}.{REGION}.run.app
#    Example: https://codementor-backend-31499486874.us-central1.run.app
#
# 4. WebSocket on Cloud Run:
#    - Use wss:// (secure websocket) instead of ws://
#    - Cloud Run supports WebSocket with timeout limits
#
# 5. Secrets Management:
#    - Use .env.local for local development (gitignored)
#    - Use Secret Manager for Cloud Run production
#    - Never commit .env.local or production secrets
#
# 6. CORS Configuration:
#    - Backend must allow requests from frontend URL
#    - Set CORS_ORIGIN in backend to frontend Cloud Run URL