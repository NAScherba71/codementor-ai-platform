# ╔═══════════════════════════════════════════════════════════════════════════╗
# ║  🚀 CodeMentor AI Engine - GCP Configuration Example                     ║
# ║  Copy this file to .env and configure for your GCP deployment            ║
# ╚═══════════════════════════════════════════════════════════════════════════╝

# ═══════════════════════════════════════════════════════════════════════════
# 🔧 Application Configuration
# ═══════════════════════════════════════════════════════════════════════════

# Flask Environment
FLASK_ENV=production
FLASK_APP=main.py

# Server Port (Cloud Run requires 8080)
PORT=8080

# Application Secret Key (use Cloud Secret Manager in production)
SECRET_KEY=your-secret-key-here-replace-in-production

# ═══════════════════════════════════════════════════════════════════════════
# 🤖 Local LLM Configuration (PRIMARY - TinyLlama Based)
# ═══════════════════════════════════════════════════════════════════════════

# Model Cache Directory
# For Cloud Run: Use /app/models (ephemeral) or mount GCS bucket
MODEL_CACHE_DIR=/app/models
HF_HOME=/app/models
TRANSFORMERS_CACHE=/app/models
TORCH_HOME=/app/models

# Chat Model (TinyLlama)
CHAT_MODEL=TinyLlama/TinyLlama-1.1B-Chat-v1.0

# Code Analysis Model (CodeT5)
CODE_MODEL=Salesforce/codet5-small

# Model Settings
MAX_LENGTH=512
TEMPERATURE=0.7
TOP_P=0.9

# Device Configuration (cpu or cuda)
DEVICE=cpu

# ═══════════════════════════════════════════════════════════════════════════
# ☁️ GCP Integration
# ═══════════════════════════════════════════════════════════════════════════

# GCP Project Configuration
GCP_PROJECT_ID=your-project-id
GCP_LOCATION=us-central1

# Vertex AI Configuration (Optional - for multi-provider support)
USE_VERTEX_AI=false
VERTEX_MODEL=gemini-pro

# Cloud Storage for Model Caching
GCS_BUCKET_NAME=codementor-models
GCS_MODEL_PATH=models/cache

# ═══════════════════════════════════════════════════════════════════════════
# 🔐 Authentication & Security
# ═══════════════════════════════════════════════════════════════════════════

# Service Account (automatically provided by Cloud Run)
# GOOGLE_APPLICATION_CREDENTIALS=/path/to/service-account.json

# API Keys (use Cloud Secret Manager in production)
# OPENROUTER_API_KEY=your-openrouter-api-key

# ═══════════════════════════════════════════════════════════════════════════
# 💾 Database Configuration
# ═══════════════════════════════════════════════════════════════════════════

# Redis Configuration
REDIS_URL=redis://localhost:6379

# MongoDB Configuration (Cloud SQL)
MONGODB_URI=mongodb://localhost:27017/codementor-ai

# ═══════════════════════════════════════════════════════════════════════════
# 📊 Monitoring & Logging
# ═══════════════════════════════════════════════════════════════════════════

# Logging Level
LOG_LEVEL=INFO

# Cloud Logging (automatically enabled on Cloud Run)
ENABLE_CLOUD_LOGGING=true

# Cloud Monitoring
ENABLE_CLOUD_MONITORING=true

# ═══════════════════════════════════════════════════════════════════════════
# ⚡ Performance Optimization
# ═══════════════════════════════════════════════════════════════════════════

# PyTorch/CPU Optimization
OMP_NUM_THREADS=2
MKL_NUM_THREADS=2

# Disable TensorFlow warnings
TRANSFORMERS_NO_TF=1
TRANSFORMERS_NO_TORCHVISION=1
TF_CPP_MIN_LOG_LEVEL=3

# ═══════════════════════════════════════════════════════════════════════════
# 🔄 Graceful Shutdown
# ═══════════════════════════════════════════════════════════════════════════

# Shutdown timeout (seconds)
SHUTDOWN_TIMEOUT=30

# Health check configuration
HEALTH_CHECK_ENABLED=1
HEALTH_CHECK_INTERVAL=30

# ═══════════════════════════════════════════════════════════════════════════
# 🎯 Multi-Provider Support (Future)
# ═══════════════════════════════════════════════════════════════════════════

# Default LLM Provider (local, gemini, openrouter)
DEFAULT_LLM_PROVIDER=local

# Provider-specific models
CLAUDE_MODEL=claude-sonnet-4.5
GPT_MODEL=gpt-5.2-codex
GEMINI_FLASH_MODEL=gemini-3-flash

# ═══════════════════════════════════════════════════════════════════════════
# 📝 Notes
# ═══════════════════════════════════════════════════════════════════════════

# 1. In Cloud Run, use Secret Manager for sensitive values:
#    - SECRET_KEY
#    - API keys (OPENROUTER_API_KEY)
#    - Database credentials
#
# 2. Model caching strategies:
#    - Option A: Download models on startup (slower cold starts)
#    - Option B: Pre-build models into Docker image (larger image)
#    - Option C: Mount GCS bucket with cached models (recommended)
#
# 3. For GPU deployments:
#    - Set DEVICE=cuda
#    - Request GPU in Cloud Run service configuration
#    - Use appropriate PyTorch/CUDA image
#
# 4. Memory requirements:
#    - TinyLlama-1.1B: ~2GB RAM minimum
#    - CodeT5-Small: ~500MB RAM
#    - Recommended: 4GB RAM for Cloud Run service
#
# 5. Cold start optimization:
#    - Keep minimum instances = 1 for production
#    - Use startup CPU boost
#    - Pre-download models to GCS bucket
