# ╔═══════════════════════════════════════════════════════════════════════════╗
# ║  🚀 CodeMentor AI Platform - GCP Deployment Configuration               ║
# ║  Copy this file to .env and configure for your GCP project               ║
# ╚═══════════════════════════════════════════════════════════════════════════╝

# ═══════════════════════════════════════════════════════════════════════════
# 🏗️ GCP Project Configuration
# ═══════════════════════════════════════════════════════════════════════════

# Your GCP Project ID (required)
GCP_PROJECT_ID=your-project-id

# GCP Region (default: us-central1)
GCP_REGION=us-central1
GCP_LOCATION=us-central1

# Deployment Environment (dev, staging, prod)
ENVIRONMENT=dev

# ═══════════════════════════════════════════════════════════════════════════
# 🐳 Artifact Registry Configuration
# ═══════════════════════════════════════════════════════════════════════════

# Artifact Registry repository name
ARTIFACT_REGISTRY_REPO=app

# Docker image registry URL (auto-generated)
# Format: {REGION}-docker.pkg.dev/{PROJECT_ID}/{REPO}
DOCKER_REGISTRY=${GCP_REGION}-docker.pkg.dev/${GCP_PROJECT_ID}/${ARTIFACT_REGISTRY_REPO}

# ═══════════════════════════════════════════════════════════════════════════
# ☁️ Cloud Run Service Names
# ═══════════════════════════════════════════════════════════════════════════

FRONTEND_SERVICE_NAME=codementor-frontend
BACKEND_SERVICE_NAME=codementor-backend
AI_ENGINE_SERVICE_NAME=codementor-ai-engine

# ═══════════════════════════════════════════════════════════════════════════
# 🤖 AI Engine Configuration (TinyLlama - PRIMARY)
# ═══════════════════════════════════════════════════════════════════════════

# Local LLM Models
CHAT_MODEL=TinyLlama/TinyLlama-1.1B-Chat-v1.0
CODE_MODEL=Salesforce/codet5-small

# Model Cache Configuration
MODEL_CACHE_DIR=/app/models
GCS_BUCKET_NAME=codementor-models
GCS_MODEL_PATH=models/cache

# AI Engine Resource Allocation
AI_ENGINE_MEMORY=4Gi
AI_ENGINE_CPU=2
AI_ENGINE_TIMEOUT=300s

# ═══════════════════════════════════════════════════════════════════════════
# 🗄️ Database Configuration
# ═══════════════════════════════════════════════════════════════════════════

# MongoDB Configuration
# Option 1: MongoDB Atlas (recommended)
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/codementor-ai

# Option 2: Cloud SQL for MongoDB
# MONGODB_URI=mongodb://username:password@cloud-sql-proxy:27017/codementor-ai

# Option 3: Local development
# MONGODB_URI=mongodb://localhost:27017/codementor-ai

# Redis Configuration
# Cloud Memorystore or local
REDIS_URL=redis://localhost:6379

# ═══════════════════════════════════════════════════════════════════════════
# 🔐 Secrets & API Keys
# ═══════════════════════════════════════════════════════════════════════════

# JWT Secret (use Cloud Secret Manager in production)
JWT_SECRET=your-super-secret-jwt-key-change-in-production

# Code Execution
JUDGE0_API_KEY=your-judge0-api-key

# OAuth
GITHUB_CLIENT_ID=your-github-client-id
GITHUB_CLIENT_SECRET=your-github-client-secret

# ═══════════════════════════════════════════════════════════════════════════
# 🧠 Vertex AI Configuration (Optional - Multi-provider support)
# ═══════════════════════════════════════════════════════════════════════════

# Enable Vertex AI APIs
ENABLE_VERTEX_AI=false

# Vertex AI Models
VERTEX_MODEL=gemini-pro
CLAUDE_MODEL=claude-sonnet-4.5
GPT_MODEL=gpt-5.2-codex
GEMINI_FLASH_MODEL=gemini-3-flash

# Budget Management
GEMINI_CODE_ASSIST_BUDGET=1138.0
GENAI_APP_BUILDER_BUDGET=1000.0
FREE_TRIAL_BUDGET=280.0

# ═══════════════════════════════════════════════════════════════════════════
# 🌐 Service URLs (Auto-populated after deployment)
# ═══════════════════════════════════════════════════════════════════════════

# These will be populated after deployment
# Format: https://{SERVICE}-{PROJECT_NUMBER}.{REGION}.run.app

# Frontend URL
FRONTEND_URL=https://${FRONTEND_SERVICE_NAME}-${PROJECT_NUMBER}.${GCP_REGION}.run.app

# Backend URL
BACKEND_URL=https://${BACKEND_SERVICE_NAME}-${PROJECT_NUMBER}.${GCP_REGION}.run.app

# AI Engine URL
AI_ENGINE_URL=https://${AI_ENGINE_SERVICE_NAME}-${PROJECT_NUMBER}.${GCP_REGION}.run.app

# ═══════════════════════════════════════════════════════════════════════════
# 📊 Monitoring & Logging
# ═══════════════════════════════════════════════════════════════════════════

# Enable Cloud Logging (recommended)
ENABLE_CLOUD_LOGGING=true

# Enable Cloud Monitoring
ENABLE_CLOUD_MONITORING=true

# Log Level
LOG_LEVEL=INFO

# ═══════════════════════════════════════════════════════════════════════════
# 🚀 Resource Allocation
# ═══════════════════════════════════════════════════════════════════════════

# Frontend Resources
FRONTEND_MEMORY=512Mi
FRONTEND_CPU=1
FRONTEND_MIN_INSTANCES=0
FRONTEND_MAX_INSTANCES=10

# Backend Resources
BACKEND_MEMORY=1Gi
BACKEND_CPU=1
BACKEND_MIN_INSTANCES=0
BACKEND_MAX_INSTANCES=10

# AI Engine Resources (configured above)
AI_ENGINE_MIN_INSTANCES=0
AI_ENGINE_MAX_INSTANCES=10

# ═══════════════════════════════════════════════════════════════════════════
# 🔄 CI/CD Configuration
# ═══════════════════════════════════════════════════════════════════════════

# Cloud Build Configuration
BUILD_TIMEOUT=1800s
BUILD_MACHINE_TYPE=E2_HIGHCPU_8

# Deployment Triggers
DEPLOY_ON_PUSH=true
DEPLOY_BRANCH=main

# ═══════════════════════════════════════════════════════════════════════════
# 🛡️ Security Configuration
# ═══════════════════════════════════════════════════════════════════════════

# CORS Configuration
CORS_ORIGIN=*

# Service Account Emails (auto-generated by Terraform)
DEPLOYER_SA_EMAIL=codementor-deployer@${GCP_PROJECT_ID}.iam.gserviceaccount.com
CLOUDRUN_SA_EMAIL=codementor-cloudrun@${GCP_PROJECT_ID}.iam.gserviceaccount.com
AI_ENGINE_SA_EMAIL=codementor-ai-engine@${GCP_PROJECT_ID}.iam.gserviceaccount.com

# ═══════════════════════════════════════════════════════════════════════════
# 📝 Notes & Instructions
# ═══════════════════════════════════════════════════════════════════════════

# 1. Initial Setup:
#    - Create GCP project: gcloud projects create PROJECT_ID
#    - Enable billing: gcloud billing projects link PROJECT_ID --billing-account=XXXXX
#    - Copy this file: cp .env.gcp.example .env
#    - Update GCP_PROJECT_ID with your actual project ID
#
# 2. Deploy Services:
#    - Run: ./scripts/deploy-gcp.sh
#    - Or use Cloud Build: gcloud builds submit
#
# 3. Secrets Management:
#    - Use Cloud Secret Manager for production secrets
#    - Set secrets: ./deploy/configure_secrets.sh
#    - Reference secrets in Cloud Run service configuration
#
# 4. Service URLs:
#    - Get URLs after deployment: gcloud run services list
#    - Or from Terraform outputs: cd infrastructure/terraform && terraform output
#
# 5. Cost Optimization:
#    - Use min_instances=0 for dev/staging (scale to zero)
#    - Use min_instances=1 for production (avoid cold starts)
#    - Monitor costs: gcloud billing accounts list
#
# 6. Monitoring:
#    - View logs: gcloud run services logs read SERVICE_NAME
#    - Cloud Console: https://console.cloud.google.com/run
#    - Metrics: https://console.cloud.google.com/monitoring
